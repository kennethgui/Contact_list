# Contact_list

### Hello!

This project is a contact list that allows the user to enter personal information of an individual, name and email, and add it to a list where the user can later edit or delete any contacts saved. This application uses a persistent database, after refreshing all data will continue to exist.

The frontend is created with react, and the back end is written in flask.
